# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# --- SkyWater PDK (downloaded by setup_tapeout.sh / run_wsl_build.sh; never commit) ---
pdk/
.volare/
volare/

# Stray PDK dir from an earlier Windows $HOME path-expansion bug
# (matches the mangled "C:Usersmouni" / "CUsersmouni" folder).
*Usersmouni/

# --- OpenLane: tool clone (repo root) + build output ---
# Anchored to root so it does NOT match open_silicon/openlane/ (Windows git is
# case-insensitive, so an unanchored "OpenLane/" would swallow it).
/OpenLane/
open_silicon/openlane/runs/

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
*.swp
